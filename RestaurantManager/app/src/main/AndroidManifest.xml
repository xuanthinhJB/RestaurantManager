<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tony.restaurantmanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeNoActionBar">
        <activity android:name=".com.example.tony.restaurantmanager.activity.LaunchScene">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".com.example.tony.restaurantmanager.activity.ManagerActivity"
            android:theme="@style/ThemeNoActionBar">
        </activity>
        <activity android:name=".com.example.tony.restaurantmanager.activity.TableDetailsActivity"
            android:theme="@style/ThemeNoActionBar">
        </activity>
        <activity android:name=".com.example.tony.restaurantmanager.dialog.AddGoodsDialog"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
        <activity android:name=".com.example.tony.restaurantmanager.dialog.AddDishDialog"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity android:name=".com.example.tony.restaurantmanager.dialog.AddMoreTable"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
        <activity android:name=".com.example.tony.restaurantmanager.dialog.ActiveNewTable"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
    </application>

</manifest>